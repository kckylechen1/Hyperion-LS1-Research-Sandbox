# Lab results (canonical location)

Ablation and Kronos probe artifacts live at the repo root:

```text
../../../data/results/
```

This mirror tree does not duplicate those files (avoids drift). See:

- `phase4_ablation_20260601_084331.*` … `phase4_ablation_20260602_005541.*`
- `kronos_causality_experiment_results_base.json` (redacted tickers)